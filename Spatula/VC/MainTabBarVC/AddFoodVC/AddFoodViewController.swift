import UIKit

class AddFoodViewController: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()
        
        // Stack Add Food Height 75 (65 + (margin 5 * 2))

//        let bodyInfoViewController = UIStoryboard.init(name: "BodyInfo", bundle: nil).instantiateViewController(withIdentifier: "BodyInfoViewController") as! BodyInfoViewController
//        self.navigationController?.pushViewController(bodyInfoViewController, animated: true)
        
//        let searchViewController = UIStoryboard.init(name: "Search", bundle: nil).instantiateViewController(withIdentifier: "SearchViewController") as! SearchViewController
//        self.navigationController?.pushViewController(searchViewController, animated: true)
        
        // Do any additional setup after loading the view.
    }
    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
